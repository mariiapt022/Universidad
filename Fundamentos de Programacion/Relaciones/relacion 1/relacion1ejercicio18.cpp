//María Peinado Toledo
//Relación 1. Ejercicio 18. 19/10/2020

#include <iostream>
using namespace std;

int main()
{
    A = X;
    B = Y;
    do {
    while (A > B) {
        A = A – B;
    }
    while (B > A) {
        B = B – A;
    }
} while (A != B);
}

//Para que el bucle termine se tiene que dar: (A>0 && B>0)||(A==B)

